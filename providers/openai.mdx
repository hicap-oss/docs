---
title: 'OpenAI'
description: 'Available Models by OpenAI'
---

<Note>The prices below are rates per 1M tokens.</Note>

## gpt-4o

GPT-4o (“o” for “omni”) is versatile, high-intelligence flagship model. It accepts both text and image inputs, and produces text outputs (including Structured Outputs). It is the best model for most tasks, and is our most capable model outside of our o-series models.

| Intelligence |  Speed   | Price (input - output) | Input | Output |
| ------------ | -------- | :---------------------: | ----- | ------ |
|   `High`   | `Medium` |       `$2.5 - $10`     | `Text, image` | `Text`  |

This model offers

> * 128,000 context window
> * 16,384 max output tokens
> * Sep 30, 2023 knowledge cutoff

Information provided from [openAI website.](https://platform.openai.com/docs/models/gpt-4o)

## gpt-4o-mini

GPT-4o mini (“o” for “omni”) is a fast, affordable small model for focused tasks. It accepts both text and image inputs, and produces text outputs (including Structured Outputs). It is ideal for fine-tuning, and model outputs from a larger model like GPT-4o can be distilled to GPT-4o-mini to produce similar results at lower cost and latency.

| Intelligence |  Speed   | Price (input - output) | Input | Output |
| ------------ | -------- | :---------------------: | ----- | ------ |
|   `Average`   | `Fast` |       `$0.15 - $0.6`     | `Text, image` | `Text`  |

This model offers

> * 128,000 context window
> * 16,384 max output tokens
> * Sep 30, 2023 knowledge cutoff

Information provided from [openAI website.](https://platform.openai.com/docs/models/gpt-4o-mini)

## gpt-4.1

GPT-4.1 is flagship model for complex tasks. It is well suited for problem solving across domains.

| Intelligence |  Speed   | Price (input - output) | Input | Output |
| ------------ | -------- | :---------------------: | ----- | ------ |
|   `Higher`   | `Medium` |       `$2 - $8`     | `Text, image` | `Text`  |

This model offers

> * 1,047,576 context window
> * 32,768 max output tokens
> * May 31, 2024 knowledge cutoff

Information provided from [openAI website.](https://platform.openai.com/docs/models/gpt-4.1)

## gpt-4.1-mini

GPT-4.1 mini provides a balance between intelligence, speed, and cost that makes it an attractive model for many use cases.

| Intelligence |  Speed   | Price (input - output) | Input | Output |
| ------------ | -------- | :---------------------: | ----- | ------ |
|   `High`   | `Fast` |       `$0.4 - $1.6`     | `Text, image` | `Text`  |

This model offers

> * 1,047,576 context window
> * 32,768 max output tokens
> * May 31, 2024 knowledge cutoff

Information provided from [openAI website.](https://platform.openai.com/docs/models/gpt-4.1-mini)

## gpt-4.1-nano

GPT-4.1 nano is the fastest, most cost-effective GPT-4.1 model.

| Intelligence |  Speed   | Price (input - output) | Input | Output |
| ------------ | -------- | :---------------------: | ----- | ------ |
|   `Average`   | `Very fast` |       `$0.1 - $0.4`     | `Text, image` | `Text`  |

This model offers

> * 1,047,576 context window
> * 32,768 max output tokens
> * May 31, 2024 knowledge cutoff

Information provided from [openAI website.](https://platform.openai.com/docs/models/gpt-4.1-nano)